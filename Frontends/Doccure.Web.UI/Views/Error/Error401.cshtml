@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>401 - Giriş Qadağandır | Doccure</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background: radial-gradient(circle at 20% 30%, #101424 0%, #070913 50%, #020308 100%);
            font-family: 'Outfit', sans-serif;
            color: #f1f5f9;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            position: relative;
        }

        .bg-glow {
            position: absolute;
            width: 600px;
            height: 600px;
            border-radius: 50%;
            filter: blur(140px);
            opacity: 0.12;
            z-index: 1;
            pointer-events: none;
            animation: float-glow 25s infinite alternate ease-in-out;
        }

        .bg-glow-1 {
            background: #6366f1;
            top: -15%;
            left: -15%;
        }

        .bg-glow-2 {
            background: #8b5cf6;
            bottom: -15%;
            right: -15%;
            animation-delay: -12s;
        }

        @@keyframes float-glow {
            0% { transform: translate(0, 0) scale(1); }
            100% { transform: translate(120px, 80px) scale(1.15); }
        }

        .error-container {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 550px;
            padding: 1.5rem;
        }

        .error-card {
            background: rgba(15, 23, 42, 0.45);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 32px;
            padding: 3.5rem 2.5rem;
            text-align: center;
            box-shadow: 0 30px 60px -15px rgba(0, 0, 0, 0.6), 
                        inset 0 1px 0 0 rgba(255, 255, 255, 0.1);
            animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) both;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .icon-container {
            width: 110px;
            height: 110px;
            margin: 0 auto 2rem;
            background: rgba(99, 102, 241, 0.1);
            border: 1px solid rgba(99, 102, 241, 0.25);
            border-radius: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            box-shadow: 0 15px 35px -5px rgba(99, 102, 241, 0.3);
        }

        .icon-container::after {
            content: '';
            position: absolute;
            inset: -5px;
            border-radius: 35px;
            border: 1px dashed rgba(99, 102, 241, 0.3);
            animation: spin 30s linear infinite;
        }

        @@keyframes spin {
            100% { transform: rotate(360deg); }
        }

        .lock-svg {
            width: 48px;
            height: 48px;
            color: #818cf8;
            animation: lock-shackle 4s ease-in-out infinite;
        }

        @@keyframes lock-shackle {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-4px); }
        }

        .error-code {
            font-size: 7rem;
            font-weight: 900;
            line-height: 1;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #a5b4fc 0%, #e0e7ff 40%, #6366f1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -3px;
            filter: drop-shadow(0 4px 15px rgba(99, 102, 241, 0.2));
        }

        .error-title {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: #ffffff;
            letter-spacing: -0.5px;
        }

        .error-desc {
            font-size: 1.05rem;
            color: #94a3b8;
            line-height: 1.6;
            margin-bottom: 2.5rem;
            font-weight: 400;
        }

        .btn-group {
            display: flex;
            gap: 1rem;
            justify-content: center;
        }

        .btn-custom {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            padding: 0.9rem 1.8rem;
            border-radius: 16px;
            font-weight: 600;
            font-size: 0.95rem;
            text-decoration: none;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            cursor: pointer;
        }

        .btn-primary-custom {
            background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
            color: #ffffff;
            box-shadow: 0 10px 25px -5px rgba(99, 102, 241, 0.4);
            border: none;
        }

        .btn-primary-custom:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px -5px rgba(99, 102, 241, 0.6);
            background: linear-gradient(135deg, #818cf8 0%, #6366f1 100%);
        }

        .btn-secondary-custom {
            background: rgba(255, 255, 255, 0.04);
            color: #cbd5e1;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .btn-secondary-custom:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(255, 255, 255, 0.2);
            color: #ffffff;
            transform: translateY(-3px);
        }

        @@media (max-width: 480px) {
            .btn-group {
                flex-direction: column;
            }
            .error-card {
                padding: 2.5rem 1.5rem;
            }
            .error-code {
                font-size: 5.5rem;
            }
            .error-title {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="bg-glow bg-glow-1"></div>
    <div class="bg-glow bg-glow-2"></div>

    <div class="error-container">
        <div class="error-card">
            <div class="icon-container">
                <svg class="lock-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"></rect>
                    <path d="M7 11V7a5 5 0 0 1 10 0v4"></path>
                </svg>
            </div>
            <h1 class="error-code">401</h1>
            <h2 class="error-title">Giriş Qadağandır</h2>
            <p class="error-desc">
                Bu səhifəyə daxil olmaq üçün yetərli səlahiyyətiniz yoxdur. Zəhmət olmasa sistemə daxil olun.
            </p>
            <div class="btn-group">
                <a href="/Auth/Login" class="btn-custom btn-primary-custom">
                    <i class="bi bi-box-arrow-in-right"></i> Giriş Edin
                </a>
                <a href="/" class="btn-custom btn-secondary-custom">
                    <i class="bi bi-house"></i> Ana Səhifə
                </a>
            </div>
        </div>
    </div>
</body>
</html>
